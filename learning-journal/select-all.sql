-- get ALL columns from `user` table;
-- asterisk (`*`) is as `ALL COLUMNS`
SELECT * FROM user;

SELECT first_name, surname FROM user;

-- get a single column in the result
SELECT first_name FROM user;
